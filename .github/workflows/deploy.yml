name: Deploy Convert

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to server
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ vars.SERVER_HOST }}
          username: opc
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -eu
            if [ ! -d ~/convert/.git ]; then
              git clone https://github.com/elaw142/convert.git ~/convert
            fi
            cd ~/convert
            git fetch --prune origin main
            git reset --hard origin/main
            git clean -fd
            docker compose up -d --build
